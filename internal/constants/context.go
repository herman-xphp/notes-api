package constants

// Context key constants to avoid magic strings
const (
	// ContextKeyUserID is the key for storing user ID in context
	ContextKeyUserID = "user_id"

	// ContextKeyRequestID is the key for storing request ID in context
	ContextKeyRequestID = "request_id"

	// ContextKeyLogger is the key for storing logger in context
	ContextKeyLogger = "logger"
)
